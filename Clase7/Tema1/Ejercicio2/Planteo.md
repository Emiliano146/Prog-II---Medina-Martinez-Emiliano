Ejercicio 2 (intermedio). Retomar EmpleadoAsalariado (Clase 6) y agregar una sobrecarga del constructor que reciba únicamente nombre y legajo, asumiendo sueldo básico y bono en 0 por defecto. Sobre la jerarquía completa, explicar en un comentario, con un ejemplo concreto de cada caso, la diferencia entre esta sobrecarga de constructor (mismo nombre, distinta firma, se resuelve en compilación) y la sobreescritura de calcularSueldo() ya existente entre superclase y subclase (misma firma, se resuelve en ejecución según el tipo real del objeto).