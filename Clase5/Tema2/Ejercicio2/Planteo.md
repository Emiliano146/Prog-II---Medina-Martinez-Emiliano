Ejercicio 2 (intermedio). Agregar a CuentaBancaria los métodos depositar(double
monto) y extraer(double monto), ambos con su propia validación (monto positivo; en
extraer, además, saldo suficiente), de forma que ninguna validación quede en main sino
encapsulada en el método correspondiente. Probar en un main una secuencia de al menos cuatro
operaciones (dos válidas y dos que deberían fallar) y capturar las excepciones esperadas mostrando
un mensaje por cada una.