Ejercicio 2 (intermedio). Modelar la clase Empleado con atributos privados nombre (String) y
legajo (int), constructor parametrizado, y un método calcularSueldo() que por ahora retorne
0 (se completará en la Clase 6). Instanciar un arreglo de al menos cuatro objetos Empleado con
datos distintos y, en un método aparte (no en main), calcular y mostrar el legajo más bajo y el más
alto del arreglo.